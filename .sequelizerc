const path = require('path')

module.exports = {
  'config': path.resolve('server/config', 'sequelize.json'),
  'migrations-path': path.resolve('server', 'migrate'),
  'seeders-path': path.resolve('server', 'seeders')
}